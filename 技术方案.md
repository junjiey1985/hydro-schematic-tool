# 水系概化图工具 — 技术方案

> 以地图为载体的水系数据管理与概化拓扑图绘制工具。
> 三大能力：**SHP 导入 / DEM 自动提取 / 手工绘制** 三种数据来源；**真实地图视图** 与 **概化拓扑图视图** 双向联动。
> 规划扩展：**模型率定**（新安江 + SCE-UA，按预报单元链式率定，见 §6）。

---

## 1. 总体架构

```
┌────────────────────────────────────────────────────────────┐
│  前端  Vue3 + OpenLayers（构建后由后端单端口托管）           │
│  ├─ 地图视图    真实坐标渲染 / SHP导入 / 绘制 / 拓扑高亮      │
│  ├─ 概化图视图  SVG 编辑器：拖动节点、河段、站点、湖泊        │
│  └─ 率定工作台  时序导入 / 参数配置 / 运行监控 / 结果对比图   │
├────────────────────────────────────────────────────────────┤
│  API 层  FastAPI（/api/projects, /layers, /topology,        │
│          /schematic, /dem, /subbasins, /timeseries,         │
│          /calibration，另含 /docs 在线文档）                 │
├────────────────────────────────────────────────────────────┤
│  核心算法  app/core/                                        │
│  ├─ shp_io      SHP 读写、投影转换（pyproj）、中文编码容错    │
│  ├─ dem         填洼 → D8 → 汇流累积 → 河网提取 → 矢量化      │
│  ├─ topology    节点-弧段模型、流向判定、站点挂接、Strahler   │
│  ├─ subbasin    子流域划分（控制断面吸附/汇流追踪/面积统计）  │
│  ├─ schematic   拓扑 → 概化图自动布局（tidy 树布局）          │
│  ├─ model/      新安江三水源产流 + 马斯京根河道演算（规划）    │
│  ├─ calibration SCE-UA 率定引擎 / 目标函数 / 评价指标（规划） │
│  └─ synth       模拟样例数据生成（DEM / SHP / 时序）          │
├────────────────────────────────────────────────────────────┤
│  存储  data/projects/<pid>/  JSON + Shapefile + 栅格缓存     │
│        + timeseries/（雨量·流量·蒸发 CSV）+ calibration/     │
└────────────────────────────────────────────────────────────┘
```

单端口部署：`npm run build` 后前端产物由 FastAPI 托管，访问 `http://127.0.0.1:8013` 即得完整应用；开发时可 `npm run dev`（Vite 5173）+ 后端，接口走代理。

## 2. 数据模型

### 2.1 图层（要素集合）

| 类型 | 几何 | 说明 |
|---|---|---|
| river | LineString | 河流水系（数字化方向即流向约定） |
| hydro_station | Point | 水文站（挂接到最近河段，可打断河段） |
| rain_station | Point | 雨量站 |
| lake | Polygon | 湖泊 / 水库 |
| boundary | Polygon | 流域边界（辅助显示） |
| subbasin | Polygon(+Point) | 子流域（预报单元）及其出口断面，由划分结果生成 |
| control_point | Point | 手工控制断面（manual 划分的单元出口） |

要素属性随层保存；`名称` 字段参与拓扑命名与概化图标注。

### 2.2 拓扑（节点-弧段）

- **打断**：所有河流线在端点与相互交点处打断，容差聚簇（默认 1 m）合并重复节点，清理假节点后得到 弧段集合。
- **节点分类**：`source`（河源，度 1 且入度 0）、`outlet`（出口）、`junction`（汇流点）。
- **流向判定**：优先使用 DEM（D8 高程下降方向 / 汇流累积量沿河递增），无 DEM 时按数字高程字段，再退化为 BFS 树定向（出口为根）。反向弧段自动翻转。
- **站点挂接**：站点投影到最近河段（距离阈值可配），记录 `edge_id + ratio`；水文站可选在挂接处打断河段，形成"站间河段"。
- **Strahler 分级**：`order = max(支流 order)，同级别汇合则 +1`。
- 每条弧段统计：长度、上游河长、上游汇水格数（有 DEM 时折算面积）。

### 2.3 概化图

由拓扑自动生成，四种元素：

- `nodes`：河源 / 汇流点 / 出口 → 圆点
- `edges`：河段 → 折线（main 实线 / extra 网河附加连线虚线）
- `stations`：站点 → 水文站▲（红）、雨量站◆（绿），默认偏置于挂接河段法线方向
- `lakes`：湖泊 → 依真实轮廓缩放的多边形

**布局算法**：以出口为根的 tidy 树布局（Reingold-Tilford 思路）——叶节点按序分配槽位、内部节点取子节点均值，层距/叶距可调；叶数过多自动压缩间距。支持 `vertical`（上游在上）/ `horizontal`（上游在左）。

**人工编辑**：所有拖动结果存入 `manual` 补丁（nodes/edges/stations/lakes 位置与拐点），重排时可选择保留；河段端点始终"粘"在节点上（`_snap_edges_to_nodes`）。

### 2.4 子流域（预报单元）

水文模型率定与实时预报不以全流域为整体，而是把流域按**控制断面**切成若干预报单元（子流域）：每个单元一套产汇流参数、一个出口断面，单元之间按上下游关系自上而下逐段演算。划分结果保存于 `project_dir/<pid>/subbasins.json`，并同步生成 `subbasin` 地图图层。

**单元字段**：`code/name/area_km2/upstream_area_km2`（单元以上累计集水面积，即率定用的控制面积）`/outlet{lon,lat,source,control,offset_m,accum_area_km2}/outlet_station/parent/children/upstream_subbasins/order_index`（排水序，上游→下游）`/chain_depth/river_length_km/edge_ids/elev_min·max·mean_m/relief_m/rain_stations`（泰森权重）/`boundary`（边界多边形）。

**划分依据（三种模式）**

| 模式 | 控制断面来源 | 典型用途 |
|---|---|---|
| station | 已挂接的水文站 | 率定：单元出口=测站，可直接用实测流量驱动参数率定 |
| junction | 拓扑汇流节点 | 预报分区细化：无站也可切分，单元数=汇流点数 |
| manual | 地图手绘 `control_point` 点 | 按实际预报断面（如水库坝址、支流入汇口）切分 |

**算法流程**（`core/subbasin.py`）

1. **重算流向场**：填洼 DEM → `d8_directions_resolved`（严格 D8 + 平地多源 BFS 解析）→ `flow_accumulation_topo`（入度+队列的拓扑序累积，避免等值平地漏算）；
2. **河道索引**：`accum ≥ channel_accum_threshold`（默认取河网提取阈值）为河道，`distance_transform_edt` 建"任意格网 → 最近河道格网"索引；
3. **控制断面吸附**：断面（站/节点/手工点）落到最近河道格网，输出偏移量，超限（>2 km）告警；mode=station 时仅接受"直接对应的站"，避免多个单元共抢同一出口站；
4. **标签传播**：种子格网按汇流累积**降序**单趟传播（下游累积必大于上游，保证下游标签不被上游覆盖），得到每格网的单元号；无效/未归属格网保持 -1；
5. **面积过滤与合并**：junction/manual 模式按 `min_area_km2` 剔除过小单元（面积=单元格网数×逐行像元面积），`merge_same_cell` 合并同格网断面；
6. **拓扑关系**：沿下游指针逐单元回溯得 `parent / upstream_subbasins / order_index / chain_depth`，出口单元为根；
7. **统计**：面积、高程分位数、河段数与河长（由 `edge_subbasin` 映射回拓扑弧段）、流域出口雨量站的最近站归属 + 逐行面积权重（泰森近似）；
8. **边界矢量化**：单元掩膜 → `rasterio.features.shapes` → 简化（`simplify_deg`）→ WGS84 多边形。

**精度说明**：真实 SRTM DEM 上黄龙滩站以上累计 11190 km²，与官方集水面积 10668 km² 偏差 +4.9%（含 DEM 分辨率与河道阈值影响）；吸附偏差 257~271 m。

**与率定 / 预报的衔接**

- **率定**：`order_index` 即演算顺序；每个单元输出 `upstream_area_km2`（控制面积）与 `rain_stations`（面雨量权重），配合出口站的实测流量做分区参数率定；
- **预报**：上游单元出流 = 下游单元入流，沿 `parent` 链逐段马斯京根/汇流曲线演算至出口；`edge_ids` 把单元与概化图河段关联，概化图即预报 的"河网骨架"；
- **概化图联动着色**：`edge_subbasin`（河段 id → 单元编号，按河段下游端点所在单元判定）驱动概化图的「河段按预报单元着色」——同一单元的河段与流向箭头同色（配色与地图单元面一致），未归属河段灰色，编辑面板内嵌单元色标图例；点击图例或侧栏「预报单元」列表行可**聚焦**（该单元河段加粗、其余淡化，视图动画定位到该单元的河段范围，再点退出并回到适配视图）；注意编号必须按排水序 `rank` 换算，不能用标签下标（曾因此 131/187 条河段归属错位）；
- **导出**：GeoJSON（边界+出口）/ CSV（率定参数表）/ SHP(zip)（GIS 入模），可直接导入模型前后处理。

### 2.5 坐标系统与底图（GCJ-02 纠偏）

- **数据坐标**：全链路统一 **WGS-84**（SHP 导入统一重投影、DEM、拓扑计算、子流域划分、落库的几何均为此坐标）；
- **底图**：国内合规底图服务——**高德地图**（矢量 `webrd` 与影像 `webst` + 注记层），通过 OpenLayers XYZ 源加载，不再使用 OpenStreetMap 等境外瓦片源；瓦片坐标为 **GCJ-02**（火星坐标），与 WGS-84 在堵河区域相差约 **535 m**（东 471 m、北 -252 m），不纠偏会整体错位；
- **纠偏策略**（`frontend/src/gcj.js`）：显示层整体转换到 GCJ-02——矢量图层逐顶点 `WGS84→GCJ02`、拓扑节点/箭头/挂接线同步转换、DEM 晕渲按四至纠偏（区域内非线性残差数十米，作背景可忽略）；交互输出（绘制/拖拽落库、选中点经纬度）转回 WGS-84；反解用迭代逼近，往返误差 < 1e-9 度；
- **密钥**：不硬编码进源码，从 `frontend/.env.local` 读取（`VITE_AMAP_KEY` / `VITE_AMAP_SECURITY_CODE`，已加入 `.gitignore`；模板见 `.env.example`）。前端产物会内联 `VITE_` 变量，属前端 Key 固有特性，正式部署应在高德控制台配置 **Referer 白名单**，或改为后端代理出瓦片。Key 类型须为「Web端(JS API)」；安全密钥仅在使用高德 JS API（地理编码等）时需要（`window._AMapSecurityConfig`），本项目底图走标准瓦片服务，不依赖 JS API。

### 2.6 时序数据（率定与预报的输入，P1 已实现）

模型率定需要**逐时段观测序列**，与空间数据分离存储：

```
data/projects/<pid>/timeseries/
├── rain/<station_id>.csv    # 雨量站逐时段降雨（mm）
├── flow/<station_id>.csv    # 水文站实测流量（m³/s）
├── evap/default.csv         # 流域日蒸发能力（mm），可多站面平均
└── manifest.json            # 时段/起止时间/缺测率/来源文件与对齐报告
```

- **导入格式**：CSV，支持长表（`station,time,value`）与宽表（首列 time，其后各列站名），时段支持 1h / 6h / 24h（日模型优先）；编码 UTF-8 / GBK 容错；
- **对齐与质检**：统一 UTC+8；以流量站为基准求各序列的公约时段与公共起止；逐站统计缺测率，>5% 告警、>20% 不可用；缺测段标记（不参与目标函数），不自动插补；
- **覆盖率检查**：每个单元按 `rain_stations` 泰森权重核对雨量序列是否齐全、出口站是否具备实测流量——不满足的单元在率定工作台中标记为"借用参数"；
- 站点 id 与图层要素 id（`H*`/`R*`）关联，站名可模糊匹配。

## 3. DEM 流水线

1. **填洼**：Priority-Flood（Barnes 2014），带 eps 抬升消除平底；
2. **D8 流向**：`d8_directions_resolved` 最陡下降 + **平地多源 BFS 解析**（`resolve_flat_directions`），把填洼平地/截断边的"无下游"格网接回排水网络（带 `z ≥ zi - tol` 防环）；
3. **汇流累积**：`flow_accumulation_topo` 按**入度+队列的拓扑序**传播（等值平地按高程排序会漏算，已弃用）；
4. **河网提取**：`accum ≥ 阈值`（默认 600 格）为河道；
5. **矢量化**：栅格线跟踪成弧段，化简 + 平滑，输出河段级矢量（附 order / 汇流面积）。

关键实现细节：填洼用**扁平数组 + 索引堆**避免 numpy 标量索引开销，过期堆键用 `processed` 标记跳过（曾因此出现假洼地导致水系断裂）。中间结果（填洼 DEM、汇流累积）以 `.npy` **无损缓存**——曾用 ASCII Grid（2 位小数）缓存抹平了 1e-4 的填洼梯度，导致严格 D8 出现 4 万+ 无下游格网的假象。GeoTIFF 读取时 `yll = t.f + height·t.e`（t.e<0），方向曾写反造成整体 195 km 偏移。矢量化河段连通性决定了"单一出口、树状河网"的正确性——调试期曾发现地形不闭合导致 47 个出口，改为**径向盆地地形**样例后归一；DEM 范围含干流下游时须用 D8 下游追踪过滤多余出口。

## 4. API 摘要

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/projects/samples/import | 一键生成示例流域（DEM+河网+站点+湖泊+拓扑+概化图） |
| POST | /api/projects/{pid}/layers/upload | SHP 上传导入（自动识别 .prj，GBK/UTF-8 容错） |
| POST | /api/projects/{pid}/layers/{lid}/features | 手绘要素保存 |
| POST | /api/projects/{pid}/topology/build | 构建拓扑（可选启用 DEM 辅助流向） |
| GET | /api/projects/{pid}/topology/geojson | 拓扑结果 GeoJSON（地图高亮渲染） |
| POST | /api/projects/{pid}/schematic/build · /reset | 生成 / 重排概化图 |
| POST | /api/projects/{pid}/schematic/edit | 增量合并拖拽编辑（nodes/edges/stations/lakes + add/remove） |
| GET | /api/projects/{pid}/schematic/export.svg | 导出 SVG |
| POST | /api/projects/{pid}/dem/upload · /extract | DEM 上传 / 河网提取 |
| POST | /api/projects/{pid}/subbasins/delineate | 子流域划分（mode=station/junction/manual，save=true 同步生成图层） |
| GET | /api/projects/{pid}/subbasins · /geojson · /options | 划分结果 / GeoJSON（边界+出口）/ 默认参数 |
| PATCH | /api/projects/{pid}/subbasins/{code} | 单元重命名（同步图层属性） |
| DELETE | /api/projects/{pid}/subbasins | 清除划分结果与图层 |
| GET | /api/projects/{pid}/subbasins/export.geojson · .csv · .shp | 导出（CSV utf-8-sig；SHP 为 zip，含边界与出口两层） |

率定相关接口见 §6.5。其中 **/timeseries 已实现（P1）**：`import`（CSV 导入，长表/宽表/单列、UTF-8/GBK 容错、时段聚合）、`manifest`（清单+单元覆盖率检查）、`{kind}/{key}`（序列读取/删除）、`demo`（合成演示数据，含真值参数）；**`/calibration` 全部已实现（P2 + P3）**：`params`/`apply`/`simulate`（P2）+ `run`/`runs`/`status`/`stop`/`result`/`convergence`（P3），率定任务经接口级验收 16/16 通过。

## 5. 已验证的端到端链路

示例流域（480×420 DEM，30 m 格网）：

- 河网提取：168 个河段 / 2560 km，单一出口，Strahler 最高 4 级
- 拓扑：169 节点（84 河源 / 83 汇流点 / 1 出口），22 个站点挂接，3 个湖泊
- 概化图：自动布局 + 拖拽保存 + SVG 导出全部通过

真实流域（堵河·黄龙滩以上，AWS Terrain Tiles 30 m）：

- 河网 / 拓扑 / 概化图生成正常，黄龙滩站吸附偏差 257~271 m
- 子流域划分（station 模式）：S01 竹山站 8211 km² → S02 黄龙滩站 2980 km² → S03 流域出口 526 km²，上下游链与排水序正确；黄龙滩以上累计 11190 km² vs 官方 10668 km²（+4.9%）
- 子流域划分（junction 模式）：94 个单元，合计面积与 station 模式一致（11716 km²），嵌套树完整
- 子流域划分（manual 模式）：手绘控制断面经吸附后正确切分；三种模式的划分/重命名/清除/三种格式导出均已验证
- 前端：划分弹窗、地图分色渲染（分色透明面 + 出口三角 + 编号/面积标注）、侧栏预报单元列表（点击定位、行点击选中、双击改名）、图例与显示开关联调通过

## 6. 模型率定模块（技术设计）

子流域划分完成后，率定的空间前提已全部就绪：每单元有**控制面积**（`upstream_area_km2`）、**面雨量权重**（`rain_stations` 泰森权重）、**演算顺序**（`order_index` / `parent` 链）与**出口测站**（`outlet_station`，station 模式）。缺的只有两块：**时序数据**（§2.6）与**模型 + 率定引擎**（本章）。

### 6.1 模型选择与结构

**新安江三水源模型**（蓄满产流）+ **马斯京根分段河道演算**，湿润半湿润流域的标准配置，与堵河（汉江上游）气候区匹配；`dt` 支持日 / 6h / 1h（日模型优先落地）。

```
单元 i（面雨量 P = Σ 权重·站雨量，蒸发 E = K·E0）
  └─ 新安江产流：蒸散发(3层) → 蓄水容量曲线(B) → 产流 R/分水源(RS/RG)
       ├─ 地表径流 RS ── 单元内河网汇流（线性水库 CS）
       ├─ 壤中流 RI ──── 线性水库 CI
       └─ 地下径流 RG ── 线性水库 CG
单元出流 Qi ── 马斯京根分段演算（KE/XE，n 按河长自动分段）── 下游单元入流
下游站模拟 Q = 演算后的 Σ 上游来流 + 本单元区间产流
```

### 6.2 参数体系（每单元一套）

下表与 `core/model/params.py` 的 `PARAM_SPEC` / `FIXED_SPEC` 一一对应（代码为准）：

| 参数 | 含义 | 初值 | 率定区间 | 敏感性 |
|---|---|---|---|---|
| K | 蒸散发折算系数 | 0.85 | 0.5 – 1.2 | 高 |
| B | 蓄水容量曲线指数 | 0.3 | 0.1 – 0.7 | 高 |
| SM | 自由水蓄水容量 (mm) | 30 | 5 – 120 | 高 |
| EX | 自由水容量分布指数 | 1.5 | 1.0 – 1.8 | 中 |
| KGF | 地下水出流比 KG/(KG+KI) | 0.5 | 0.25 – 0.75 | 中 |
| CG | 地下水库消退系数 | 0.985 | 0.93 – 0.998 | 高 |
| CI | 壤中流消退系数 | 0.7 | 0.4 – 0.9 | 中 |
| CS | 河网汇流消退系数 | 0.3 | 0.05 – 0.6 | 高 |
| XE | 马斯京根 XE | 0.2 | 0 – 0.4 | 中（仅演算河段） |
| KE | 马斯京根 KE (h) | 按河长自动估算 | 不率定（可由出口间距覆盖） | — |

固定参数（不率定，区域经验值）：**WM = 140 mm**（张力水蓄水容量，其中 UM = 20、LM = 60、DM = 60）、**C = 0.15**（深层蒸散发系数）、**IMP = 0.01**（不透水面积比）、**KSS = 0.7**（自由水总出流系数 KG + KI）。

自由度控制：每单元**参与率定 8–9 个参数**（K、B、SM、EX、KGF、CG、CI、CS，外加 XE——仅当该单元有直接上游时开放）；WM/C/IMP/KSS 固定。KE 由河长 `river_length_km` 与波速估算初值。

### 6.3 率定策略（单元链式，自上而下）

按 `order_index` 排水序逐单元推进，**下游率定时上游参数已固定**（演算过的出流作为上边界），每步自由参数只属于一个单元，避免高维联合优化：

1. **首个有站单元**（如堵河 S01·竹山站）：以本站实测流量为目标，率定该单元 8 参数（无上游来流 → XE 不可辨识，不开放）；
2. **下游有站单元**（S02·黄龙滩站）：模拟 = 演算(上游已率定出流) + 本单元区间产流，目标仍为本站实测；此时 XE 参与率定（9 参数）；
3. **无站单元**：不独立率定，参数**借用**直接下游已率定单元（结果表标注 `borrowed_from` 与原因）；
4. **联合率定**（可选扩展）：多站目标加权 `F = Σ wᵢ·(1−NSEᵢ) + λ·|PBIAS|`，所有单元参数同时优化——自由度大，需配合**参数共享分组**（如全流域共享 B/EX，仅 K/SM 分单元），作为 P5 阶段能力；
5. **期划分**：序列按 7:3 切分率定期/验证期（`split` 可配，钳制在 [0.3, 0.95]），验证期只评估不再调参；
6. **最终复算**：链式跑完后用最终参数集调用 `simulate_basin` 整体复算一遍作为**权威结果**——收敛史里的参数与实际产出过程线严格一致，"率定结果"与"模型模拟"面板完全同源。

### 6.4 率定算法与目标函数

- **算法**：SCE-UA（Duan 1992，纯 numpy 实现，见 `core/model/calibrate.py`）。初始种群均匀随机撒点，**复合体数 `s = p`、每复合体 `m = 2p+1` 点、子复合体 `q = p+1` 点**，梯形概率选点，反射 → 收缩 → 随机变异三步替换最差点，连续块划分复合体，每复合体每次大循环演化 `m` 次；终止条件为连续 `k=5` 代最优值改进 < `1e-4` 或达最大评估次数（默认 3000）。**未做 multiprocessing 并行**——单次评估已优化到 4.6 ms，且链式率定各单元串行依赖，并行的收益不足以抵消进程开销；
- **目标函数**（单站）：`F = (1 − NSE) + 0.3·|PBIAS|/100 + 0.1·RMSE/std(obs)`，缺测时段剔除；多站按控制面积加权（P5）；
- **评价指标**：NSE、R²、KGE、RMSE、PBIAS（水量平衡相对误差）、峰现时间误差——率定期与验证期分列；
- **峰现时间误差**：连续多年的长序列若只取全局最大值，"峰现时间误差"会被"哪一年最大"主导（实测曾报 −413 时段，无实际意义）。实现中改为**多场洪峰统计**：取实测最显著的 5 场洪峰（数值 ≥ 1.5×序列均值、相互间隔 ≥ 10 时段），在每场峰现时刻 ±15 时段内寻找模拟峰值，输出各场误差（时段数）与**中位数**，并把时段序号换算为实际时刻返回；
- **后台运行**：FastAPI 后台线程执行，进度（当前单元 / 代数 / 已评估次数 / 当前最优 F / 已完成单元）写 `runs/<rid>/progress.json`，前端轮询；`stop` 在每代边界响应，结果仍落盘可读。

### 6.5 API

| 状态 | 方法 | 路径 | 说明 |
|---|---|---|---|
| ✅P1 | POST | /api/projects/{pid}/timeseries/import | 上传雨量/流量/蒸发 CSV（kind + 时段），返回对齐与质检报告 |
| ✅P1 | GET | /api/projects/{pid}/timeseries/manifest | 数据清单 + 各单元覆盖率检查结果 |
| ✅P1 | GET/DELETE | /api/projects/{pid}/timeseries/{kind}/{key} | 读取（可抽稀）/ 删除序列 |
| ✅P1 | DELETE | /api/projects/{pid}/timeseries | 清空（可指定 kind） |
| ✅P1 | POST | /api/projects/{pid}/timeseries/demo | 生成合成率定数据（见 §6.8 说明，流量由真值模型跑出） |
| ✅P2 | GET | /api/projects/{pid}/calibration/params | 单元参数集 + 参数规范（区间/标签）+ 固定参数，供前端表单 |
| ✅P2 | POST | /api/projects/{pid}/calibration/apply | 保存参数集为项目默认（`{params:{code:{...}}}`）、`{reset:true}` 恢复默认、`{run_id:"..."}` 采纳某次率定结果 |
| ✅P2 | POST/GET | /api/projects/{pid}/calibration/simulate | 全流域模拟（可传 params/period）→ 过程线 + 指标 + 水量平衡 |
| ✅P3 | POST | /api/projects/{pid}/calibration/run | 启动 SCE-UA 链式率定（后台线程），返回 run_id；body `{period,split,max_evals,seed,tol,lock}` |
| ✅P3 | GET | /api/projects/{pid}/calibration/runs | 历次任务列表（新→旧，含 config/progress 摘要与 has_result） |
| ✅P3 | GET | /api/projects/{pid}/calibration/runs/{rid}/status | 进度（phase/unit/gen/evals/best_f/units_done）+ `alive`；服务重启丢失的任务标 failed |
| ✅P3 | POST | /api/projects/{pid}/calibration/runs/{rid}/stop | 终止任务（当前代结束后退出）；已结束任务返回 409 |
| ✅P3 | GET | /api/projects/{pid}/calibration/runs/{rid}/result | 最终参数集 + 逐单元收敛史 + 分期指标 + 权威全流域复算；未完成返回 409 |
| ✅P3 | GET | /api/projects/{pid}/calibration/runs/{rid}/convergence | 轻量接口，仅返回逐单元收敛曲线（画进度图不拖全量结果） |
| P5 | GET | /api/projects/{pid}/calibration/export.csv | 参数表导出（单元关联，utf-8-sig） |

**simulate 返回结构**：`{ok, dt_s, n_steps, period{start,end,warmup_steps}, units[{code,name,area_km2,params,upstream,outlet_station,balance,metrics,series{time,sim,obs}}], water_balance[], warnings[]}`；`balance` 含 `sum_p/sum_e/sum_r/sum_q/storage_change/closure/closure_rate`，`closure_rate = 闭合误差/降水总量`。

### 6.6 前端「率定工作台」

**P2 已交付「模型模拟」面板**（顶栏「模型模拟」按钮 → 弹窗，未引入图表库）：① 参数集矩阵表（行=参数、列=单元，可编辑、超区间标红并拦截运行，KE 只读展示）+ 固定参数说明；② 模拟时段（起止 / 预热天数，默认取时序数据范围）与「运行模拟」；③ 结果——单元切换标签（含 NSE 徽标）、指标卡（NSE/R²/KGE/RMSE/PBIAS/峰现时间误差）、实测 vs 模拟过程线（内联 SVG，双曲线 + 网格）、洪峰明细表、水量平衡表（P = E + q + Δ蓄量，闭合误差 / 径流系数 / 闭合率）；支持「恢复默认参数」与「保存为项目参数」。

P4 将在此基础上扩成四步式工作台：

1. **数据检查**：manifest 覆盖率表格（每单元：面雨量站齐备？出口站有流量？），缺测高亮，不满足单元标"借用参数"；
2. **配置**：时段选择（率定期/验证期起止）、模型参数初值与区间表（可锁定个别参数）、站点权重；
3. **运行**：进度条 + 迭代收敛曲线（SCE-UA 目标值逐代下降），支持终止；
4. **结果**：逐站实测 vs 模拟过程线对比图（率定期/验证期切换）、指标卡（NSE/PBIAS/RMSE）、参数表（可编辑单值后"仅模拟"复核）→「保存为项目参数」。

过程线图表引入 **ECharts**（按需引入 Line，gzip 增量约 +110 KB；P4 时替换当前 SVG 实现并增加缩放/悬停/率定期-验证期切换）；参数编辑表沿用子流域弹窗的表格风格。概化图/地图联动：结果参数表的单元行点击 → 聚焦对应单元（复用 `focusSubbasinInSchematic`）。

### 6.7 存储与性能

```
data/projects/<pid>/calibration/
├── default.json          # 当前采用的参数集（单元 code → 8 参数 + 固定参数快照）
├── demo_truth.json       # 演示数据的真值参数存档（OSSE，供率定回收比对）
└── runs/<rid>/           # config.json（含单元清单）+ progress.json（逐代落盘）+ result.json
```

- **性能预算（P3 实测）**：numpy 数组 + 逐时段 Python 循环。`simulate_basin` 单次全流域（堵河 3 单元 × 1095 天日模型）≈ 0.64 s，但其中模型核仅 6 ms，**99% 耗时在面雨量/蒸发/河道 KE 的装配**；因此率定走**预计算上下文**热路径（`unit_context` 一次性装配 → `eval_unit` 只跑模型核），单次目标评估 **≈ 4.6 ms**（相对 `simulate_basin` 提速约 140×，链式单单元结果与全流域复算逐位一致，差 0.00e+00）。于是 800 次评估/单元 ≈ 5–7 s、3000 次评估/单元 ≈ 15–20 s，**链式 3 单元全链 < 1 分钟，无需引入 numba**。预测：30 年日模型（10950 步）单次评估 ≈ 45 ms，3000 次评估 ≈ 2.5 min/单元，仍可接受；
- 若后续提频（1h）或大流域卡顿，可将模型核改写为 numba `@njit`（接口不变）；
- **可复现性**：每次 run 落盘 config（参数区间/种子/时段）+ progress（逐代）+ result（收敛史/参数/指标/过程线），实验可回放。
- **后台执行**：率定为后台线程 + 进度逐代落盘，前端轮询 `status`；`stop` 在当前代结束后退出，已完成单元的参数保留，未跑到的单元在结果里以 `status=not_run` 占位（前端表格仍完整）。服务重启会丢线程，`status` 接口据 `alive` 如实标注 failed，避免前端无限轮询。

### 6.8 分期实施

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| P1 ✅ | 时序数据导入与管理（§2.6） | 已完成：CSV 导入（长表/宽表/单列、GBK 容错、时段聚合）+ manifest 覆盖率报告 + 合成演示数据，接口与前端面板均经浏览器实测 |
| P2 ✅ | 新安江 + 马斯京根模拟引擎、simulate API 与「模型模拟」面板 | **已完成**：`core/model/{xaj,routing,metrics,params,simulate}.py` + `/calibration/{params,apply,simulate}` + 前端参数/过程线/水量平衡面板。验收实测（堵河 3 单元 × 1095 天）：水量平衡闭合误差 = 0（机器精度，远优于 <1% 的目标）；默认参数 NSE 0.959 / 0.965 / 0.959、KGE ≈ 0.85；真值参数 NSE 0.995、PBIAS < 0.2%（证明模型与数据自洽）；单次模拟 0.64 s；面板经真实浏览器验证（参数编辑、单元切换、过程线、洪峰表、水量平衡表） |
| P3 ✅ | SCE-UA 链式率定 + 指标评估 | **已完成**：`core/model/calibrate.py`（SCE-UA + 链式率定）+ `/calibration/{run,runs,status,stop,result,convergence}`。接口级验收 16/16 通过（另有终止语义 8 项边界断言）：800 次评估/单元 × 3 单元全链 18.0 s；**率定期 NSE 0.9944–0.9950、验证期 NSE 0.9929–0.9953**（目标 > 0.9）；高敏参数（K/B/CG/CI/KGF）真值回收偏差 < 6%；采纳参数后默认模拟 NSE 0.9942–0.9949；终止任务 6.6 s 内响应、结果仍可读且单元补 `not_run` 占位 |
| P4 | 前端率定工作台四步流程（在 P2「模型模拟」面板上扩展） | 全流程 UI 走通，结果图表（ECharts）与参数保存 |
| P5 | 联合率定、参数共享分组、导出、GLUE 不确定性（可选） | 多站联合目标收敛、导出表入模 |

**P3 关键实现要点**

1. **链式而非联合**：按 `order_index` 排水序自上而下逐单元率定。率定某单元时其直接上游参数已定，上游出流经马斯京根演算后作为**已知入流**叠加到本单元出流（`q_out = simulate_unit(...) + Σ muskingum_route(up.q, KE, XE, dt)`）——每步只优化 8–9 个参数，避开 94 单元联合优化的维度灾难。最终用**最终参数集整体复算**（复用 `simulate_basin`）作为权威结果，保证"率定结果"与"模型模拟"面板完全同源，不会出现两套实现漂移；
2. **自由参数集**：8 个产流参数恒开放；`XE`（马斯京根流量比重因子）**仅当该单元有直接上游**才开放——无来流时该参数不可辨识，强行率定会让 SCE-UA 在无效维度上浪费评估次数（首单元 S01 已验证：自由集 8 个，无 XE）。`lock` 可把指定参数固定在给定值；
3. **SCE-UA 标准实现**（Duan 1992）：复合体数 `s = p`、每复合体 `m = 2p+1` 点、子复合体 `q = p+1` 点，**梯形概率**选择子复合体，反射 → 收缩 → 随机变异三步替换最差点，连续块划分复合体，每复合体每次大循环演化 `m` 次；
4. **目标函数** `F = (1−NSE) + 0.3·|PBIAS|/100 + 0.1·RMSE/std(obs)`，在**率定期**上计算；验证期只评估不再调参（`split` 默认 0.7，钳制在 [0.3, 0.95]）；
5. **无出口实测的单元**：不参与率定（链上演算先用默认参数，其误差被下游率定吸收），**最终参数借用直接下游已率定单元**，并在结果中标注 `borrowed_from` / `status=skipped`；
6. **终止与容错**：`stop_flag` 在每代边界检查（不打断单次评估，`eval_unit` 仅 4.6 ms 故响应及时）；SCE-UA 内部对数值异常的坏点返回 `1e6` 而非崩溃；结果落盘用 `json.dump(default=str)`，`datetime`/`numpy` 标量不会让整份结果丢失；
7. **峰现时间误差**：长序列取全局最大值会落到不同年份（曾报 −413 步，无意义）→ 改为**最显著 5 场洪峰**（≥1.5× 均值、间隔 ≥10 时段）在 ±15 时段窗口内匹配，取**中位数**；率定期/验证期分别统计。

**P2 关键实现要点**

1. **水量平衡严格闭合**：三个线性水库采用**蓄量型**写法 `S ← k·S + in`、`out = (1−k)·S_prev`（入流 + 前蓄量 ≡ 新蓄量 + 出流），而非递推滤波式 `Q = k·Q_prev + (1−k)·I`——后者的"虚拟蓄量"不入账，会造成 −25% 量级的水量泄漏。逐时段恒等式 `ΣP = ΣE + Σq + Δ蓄量`，实测闭合为机器精度；
2. **张力水扣减不能截断符号**：净雨 `PE = P − E` 为负时（干旱期蒸散发消耗土壤水）必须原样传入产流模块扣减张力水蓄量，早期写 `max(0, PE)` 导致张力水永不消退、水量严重泄漏；
3. **不透水面积**：`IMP·P` 从净雨中扣除后直接成流，但保留 `PE` 的符号，参与全局平衡；
4. **演算河段 KE 由空间距离自动估算**（弯曲系数 1.3、波速 1.5 m/s），单段 KE 受稳定性约束钳制 ≥ Δt/2，分段数 `n = ceil(KE/(Δt/2))`。

**样例数据（OSSE）**：`POST /timeseries/demo` 生成的"实测"流量不是随便造的——先用**真值参数跑一遍本工具的新安江 + 马斯京根模型**（观测系统模拟实验），再叠加 5% 乘性噪声写入流量序列，真值参数存 `calibration/demo_truth.json`。这样 P3 率定有明确的可回收基准（真值参数 NSE = 0.995 → 率定应收敛到同量级）。雨量站间差异、年周期湿季权重、少量暴雨均为合成；蒸发能力年均 ≈ 1.8 mm/d（≈660 mm/年），黄龙滩站合成流量均值 116 m³/s、年径流深 343 mm（实测 111 m³/s / 328 mm，量级一致）。**注意**：合成数据的 NSE 天然偏乐观，不足以评价模型结构。

## 7. 边界与后续方向

- 汇流累积量折算面积依赖格网尺寸，建议 DEM 统一投影后使用；
- 大型 DEM（>4000×4000）建议分块或提高阈值，填洼为纯 Python 堆实现，可换 richdem/WBT 加速；
- `unassigned` 面积（DEM 范围大于实际流域的未归属区）目前仅在 stats 中单列，未做掩膜裁剪；
- 率定模块按 §6 设计实施：**P1（时序数据）、P2（模拟引擎）、P3（SCE-UA 链式率定）已完成**，下一步 P4（前端率定工作台四步流程）；实时预报（沿 `parent` 链逐段演算至出口、接入实时雨情）在率定参数稳定后启动；
- 网河/分汊的 `extra` 连线目前仅显示不参与主树布局；
- 多人协作可平滑升级到 PostGIS + 项目权限。
